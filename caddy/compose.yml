services:
  caddy:
    image: caddy:alpine
    restart: unless-stopped
    ports:
      - "${HOST_PORT}:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    depends_on:
      irid:
        condition: service_healthy
